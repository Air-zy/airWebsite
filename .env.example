airKey=personal key
airWebToken=robloxstuff
discord=discordlink
discordName=discordname
dwebhook=webhook
email=email
firebaseJsonKey=long firedb pass
firebaseJsonKey2=second firedb pass
azyFirebaseJSON=avy firebase json
location=personal location
publicClusterKey=public cluster key
airClusterAcolyteToken=personal pass for the cluster
airWebToken2=for pushing in github repo
repoPat=github repo pat
HFSpacesSecret=hugging face spaces secret

whookPass=webhook password
wn_dwebhook=webhook

UTIL_DB=encrypted postgres url, paste neon's connection string as is

rowaCloudApi=longrobloxcloudapikey

# auth. sessionSecret is required, the server will not boot without it.
# generate a random string, encrypt it with airKey, paste the ciphertext here.
sessionSecret=encrypted random string for signing session cookies

# optional, both encrypted with airKey. without them continue with google says its not set up.
# from a web oauth client in google cloud console, redirect uri is <site>/auth/google/callback
googleClientId=encrypted google oauth client id
googleClientSecret=encrypted google oauth client secret
